
// TODO : ADD REDIS COMMUNICATION
const initializeRedisPubSub = ( subnets, hosts )  => {
}

const publishToMTC = ( subnets, hosts )  => {
  console.log('\n publishToMTC')
}

const subscribeToMTC = ( subnets, hosts )  => {
  console.log('\n subscribeToMTC')
}